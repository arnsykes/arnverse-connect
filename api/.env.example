# ===================================================================
# ARNVERSE API Environment Configuration
# Salin file ini ke .env dan sesuaikan dengan konfigurasi server Anda
# ===================================================================

# Database Configuration
DB_HOST=localhost
DB_NAME=arnn8651_arnverse
DB_USER=arnn8651_arnverse
DB_PASS=your_database_password_here

# JWT Secret Key (WAJIB diubah untuk production!)
# Generate random string yang kuat untuk keamanan
JWT_SECRET=arnverse_jwt_secret_key_2024_change_in_production_to_random_string

# Base URL Configuration
BASE_URL=https://arnworld.space

# Optional: Override timezone (default: Asia/Jakarta)
# TIMEZONE=Asia/Jakarta

# Optional: Custom upload limits (default dalam _env.php)
# UPLOAD_MAX_SIZE=10485760
# STORY_EXPIRE_HOURS=24
# JWT_EXPIRE_HOURS=168

# Optional: Pagination defaults
# DEFAULT_PAGE_SIZE=20
# MAX_PAGE_SIZE=100

# Optional: Rate limiting (untuk production dengan Redis)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# Debug Mode (set ke false untuk production)
DEBUG_MODE=false

# ===================================================================
# PETUNJUK PENGGUNAAN:
# 1. Salin file ini ke .env (tanpa .example)
# 2. Ubah semua nilai sesuai dengan konfigurasi server Anda
# 3. Pastikan JWT_SECRET adalah string random yang kuat
# 4. Jangan commit file .env ke version control
# 5. Set permission file .env ke 600 untuk keamanan
# ===================================================================